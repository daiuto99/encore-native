package com.encore.core.data

/**
 * App-wide constants shared across modules.
 */
object AppConstants {
    /**
     * Placeholder userId used for single-user local storage.
     * Replace with real user identity when multi-user / backend auth is introduced (Track 6).
     */
    const val LOCAL_USER_ID = "local-user"
}
